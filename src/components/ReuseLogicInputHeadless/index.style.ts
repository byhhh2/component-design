import styled from '@emotion/styled';
import InputWrapper from '.';

export const Input = styled(InputWrapper.Input)`
  color: blueviolet;
`;

export const Label = styled(InputWrapper.Label)`
  color: yellowgreen;
`;
